# Meowmoji 测试工作原则（Cursor 执行基准）

> **状态：** live · **更新：** 2026-06-24  
> **框架来源：** Vibe Coding 项目测试用例设计原则（完整收录）  
> **适用范围：** Cursor 全部测试工作 · 产品仅真机验收（ADR-011）  
> **策略索引：** [docs/live/TEST.md](../docs/live/TEST.md)

---

## 0. 分工与文档真源

| 角色 | 职责 |
| --- | --- |
| **产品** | 真机验收 · 飞书填表 · 提供版本/时间/截图 |
| **Cursor** | 用例设计 · 开发者工具/CMS/CI · Bug 排查修复 · 文档维护 |
| **Codex** | 不再维护用例 |

| 文档 | 用途 |
| --- | --- |
| **本文件** | 设计原则（AI 必须严格遵守） |
| [TEST-SPEC-v3.md](./TEST-SPEC-v3.md) | 用例规格真源（275 条 · Gate 170） |
| [TEST-PRIORITY-v3.md](./TEST-PRIORITY-v3.md) | 优先级定义 |
| [TEST-ACCEPTANCE-PATHS-v3.md](./TEST-ACCEPTANCE-PATHS-v3.md) | 产品真机 P0 路径 |
| [TEST-SCHEDULE-v3.md](./TEST-SCHEDULE-v3.md) | D1–D3 排期 |
| [feishu-gate-v3.md](./feishu-gate-v3.md) | Gate 填表镜像 |
| [TRACEABILITY.md](./TRACEABILITY.md) | 验收 ↔ 单元/集成映射 |
| [BUG-INVESTIGATION-PRINCIPLES.md](../docs/live/BUG-INVESTIGATION-PRINCIPLES.md) | Bug 五阶段详版 |

---

# 一、顶层核心原则（AI 必须严格遵守）

| # | 原则 | 要求 |
| --- | --- | --- |
| 1 | **业务优先** | 围绕用户真实使用场景；验收层步骤不出现 API/云函数/数据库术语 |
| 2 | **全覆盖** | 每条功能链：**正常 → 逆向 → 边界 → 异常** 全链路 |
| 3 | **路径穷尽** | 主线、支线、分支、循环、异常退出均有对应用例 |
| 4 | **可验证** | 预期可量化、可执行、可重复；禁止空泛「正常/一致」 |
| 5 | **独立性** | 原子用例可单独 fail；依赖须写 seed + 清理 |
| 6 | **优先级分层** | P0–P3；**Gate = P0+P1（170）** → [TEST-PRIORITY-v3.md](./TEST-PRIORITY-v3.md) |

**版本迭代分工（2026-06-23）：**

| 环节 | 负责 |
| --- | --- |
| 用例编写 + 自动化/点测执行 | **Cursor** |
| 真机验收结论 | **产品**（飞书或口头确认） |
| 已全量功能（如 v1.0.51 Gate3） | 不以 Gate 表重复填；新功能单独约定是否进 TEST-SPEC |
| **广场 v2** | 已随正式版上线；**不走 Gate 表逐项** · 产品后续单独验收 |

**双层模型：**

| 层 | 文档 | 说明 |
| --- | --- | --- |
| 原子用例 | TEST-SPEC-v3 | 独立编号、完整字段 |
| 旅程脚本 | TEST-ACCEPTANCE-PATHS + 飞书 | 真机按序，引用原子 ID |

---

# 二、分层测试用例框架（按测试层级）

| 层级 | 定义 | Meowmoji 落点 | 用例前缀/文档 |
| --- | --- | --- | --- |
| **1. 单元测试** | 核心函数/方法/类 IO；分支/条件/循环；合法/非法/空/默认；异常抛出与捕获 | `admin-api/test/*`、`admin-web/**/*.test.ts` | TRACEABILITY 映射，不写进步骤正文 |
| **2. 集成测试** | 前后端调用正常/异常；DB/缓存/存储一致性；第三方（AI/登录/邮件）；模块间数据格式 | staging API + 云函数链路 | Cursor CI + 手工补证 |
| **3. 系统测试** | 完整用户操作；模块交互；多环境；**含非功能** | 开发者工具 + CMS 浏览器 | MP-F* / CMS-C* / SCN-* |
| **4. 验收测试** | PRD 功能点；核心旅程 E2E；易用性；上线冒烟 | **产品真机** + Cursor 填表 | E2E-* + 飞书路径 |

**设计 v3 时：功能用例（F/C）与非功能/边界专项（BND/EXC/NFR/SCN）必须分 catalog 齐全，禁止只有「 happy path 功能用例」。**

---

# 三、核心测试维度（AI 必须全部覆盖）

## ✅ 维度 1：正常流程

**定义：** 用户按预期操作，系统返回正确结果。

须覆盖：主线路径 · 支线路径 · 多角色正常操作 · 多步骤连续 · 数据增删改查（用户视角）。

**v3 catalog：** `MP-F*` / `CMS-C*` 中 **测试维度=正常**

---

## ❌ 维度 2：逆向流程

**定义：** 用户错误或非法输入时的容错。

| 子类 | 须覆盖示例 |
| --- | --- |
| 输入错误 | 空输入、超长、特殊字符、格式错误、非法数值、重复/已存在 |
| 操作错误 | 跳过前置步骤、重复点击、快速连点、中途取消、回退、权限不足 |
| 系统错误 | 断网、超时、网络切换 |
| 依赖错误 | 第三方不可用、资源不存在/已删 |

**v3 catalog：** `MP-F*` / `CMS-C*` 中 **测试维度=逆向** + `BND-*` 极值逆向

---

## 🧱 维度 3：边界条件

**定义：** 临界值、极限值附近的表现。

| 子类 | 须覆盖 |
| --- | --- |
| 数值边界 | min、max、min−1、max+1、0、1、−1、空、默认 |
| 容量边界 | 最大文件/文本长度、最多列表项、并发上限 |
| 时间边界 | 开始/结束/截止、跨天/月/年、超时、定时触发 |
| 权限边界 | 未登录/用户/运营/超管边界 |

**v3 catalog：** `BND-MP-*` / `BND-CMS-*`（**专项，不可仅散落在功能用例**）+ 功能块内关键边界

**Meowmoji 强制边界清单：**

- 上传 7/8/16/17 张 · 作品名 0/1/8/9 字 · 公告标题 19/20/21 字  
- OTP/登录限频 · 5 次锁定 15 分钟 · 密码 ≥8 位含字母数字  
- 次数 0/1/N · 跨天恢复 · 反馈/昵称/搜索长度极值  

---

## ⚠️ 维度 4：异常场景

**定义：** 极端情况下的稳定性与容错。

须覆盖：崩溃/杀进程恢复 · 强制关闭后状态 · 并发/数据竞争 · 恶意输入（XSS/SQLi 形） · 异常文件 · 会话中断。

**v3 catalog：** `EXC-*` + `MP-F8-*` + 各块 **测试维度=异常**

---

## 🔄 维度 5：路径覆盖

**定义：** 所有业务分支与组合路径。

须覆盖：分支 true/false · 循环 0/1/多次 · 异常退出后再进入 · 多分支组合（如 A→B→C 切换再生成）。

**v3 catalog：** `PATH-*` + 功能块 **测试维度=路径** + TEST-ACCEPTANCE-PATHS 旅程

---

## 🌐 维度 6：全量场景

**定义：** 角色 × 环境 × 设备 × 网络 × 数据量 × 操作顺序。

| 子类 | 须覆盖 |
| --- | --- |
| 角色 | 游客、普通用户、运营、超管 |
| 环境 | staging（验收基线）；生产排期另列 |
| 设备 | iOS/Android；小屏/大屏；CMS Chrome/Safari/Firefox/Edge |
| 网络 | WiFi、4G、弱网、断网、切换 |
| 数据 | 空、少量、大量（10+ 作品、50+ 消息）、脏数据 |
| 操作顺序 | 逆序、中断后继续 |

**v3 catalog：** `SCN-*`

---

# 四、非功能测试用例框架（验收阶段必测）

**与功能用例分 catalog，禁止合并为一条「顺便测性能」。**

| 类型 | 覆盖项 | v3 前缀 |
| --- | --- | --- |
| **1. 性能** | 页面加载、接口响应、首屏反馈、长列表滚动、长时间运行 | `NFR-PERF-*` |
| **2. 安全** | 认证/授权/会话、OTP 限频、锁定、数据隔离、XSS 输入、敏感操作 | `NFR-SEC-*` |
| **3. 兼容性** | 浏览器四件套、OS、移动设备、分辨率、弱网 | `NFR-COMPAT-*` |
| **4. 易用性** | 布局合理、错误文案（见 ERROR-COPY 原则）、空态/loading、主路径步数 | `NFR-UX-*` |

---

# 五、Meowmoji v3 用例 catalog 与编号

| 前缀 | 类型 | 说明 |
| --- | --- | --- |
| `MP-F{n}-{seq}` | 功能·小程序 | F1–F10 用户功能 |
| `CMS-C{n}-{seq}` | 功能·CMS | C1–C8 运营功能 |
| `BND-MP-{seq}` | **边界专项·小程序** | min/max±1、空、0、1、长度极值 |
| `BND-CMS-{seq}` | **边界专项·CMS** | 同上 |
| `EXC-{seq}` | **异常/稳定性** | 并发、崩溃、恶意输入、会话 |
| `PATH-{seq}` | **路径组合** | 分支组合、循环 0/1/N、回退再进 |
| `SCN-{seq}` | **全量场景** | 角色×环境×设备×数据量 |
| `E2E-{seq}` | 跨端联调 | 端到端旅程 |
| `NFR-PERF-{seq}` | 性能 | |
| `NFR-SEC-{seq}` | 安全 | |
| `NFR-COMPAT-{seq}` | 兼容性 | |
| `NFR-UX-{seq}` | 易用性 | |

---

# 六、用例必填字段

```
编号 · 名称
- 优先级：待定（定稿后统一）
- 用例类型：功能 | 边界 | 异常 | 路径 | 场景 | 联调 | 非功能
- 测试维度：正常 | 逆向 | 边界 | 异常 | 路径 | 联调 | 全量场景 | 非功能
- NFR 子类：（非功能时）性能 | 安全 | 兼容 | 易用性
- 角色：
- 独立执行：是 | 否（seed + 清理）
- 可量化标准：
- 验收路径：
- 产品范围 ID：
- 前置条件 / 操作步骤 / 预期结果
```

---

# 七、每功能块最低覆盖（设计自检）

| 检查项 | 要求 |
| --- | --- |
| 功能 catalog | 每 F/C 块：正常≥1、逆向≥2、边界≥1、异常≥1（有外部依赖）、路径≥1 |
| 边界 catalog | BND-MP + BND-CMS 覆盖 §三 强制边界清单 |
| 非功能 catalog | NFR-PERF/SEC/COMPAT/UX 各 ≥8 条 |
| 全量场景 | SCN ≥10 条 |
| 异常专项 | EXC ≥10 条 |
| 路径专项 | PATH ≥5 条 |
| 可量化 | 100% 有「可量化标准」 |

---

# 八、Bug 排查与修复原则

> 详版：[BUG-INVESTIGATION-PRINCIPLES.md](../docs/live/BUG-INVESTIGATION-PRINCIPLES.md)

**核心信条：** 证据先于结论 · 分层定位 · 文案是线索不是结论 · 找不到就继续挖或求助

**五阶段（禁止跳过 ①② 直接改代码）：**

```
① 对齐现象 → ② 假设与排查方向 → ③ 取证 → ④ 下结论 → ⑤ 修复与防复发
```

| 阶段 | 做什么 | 禁止 |
| --- | --- | --- |
| ① 对齐现象 | 版本、时间（到分钟）、路径、设备、错误原文 | 把用户描述当根因 |
| ② 排查方向 | 2–4 条假设 + 第一条证据 | 跳过直接猜配置/改代码 |
| ③ 取证 | 日志、状态码、DB、复现 | healthz/错误页当最终结论 |
| ④ 下结论 | 证据支持 + 互斥假设否定 | 证据不足写「已定位」 |
| ⑤ 修复 | 最小改动 + 同观测面验证 + 更新用例 | 只改文案不修根因 |

---

# 九、Cursor 工作流

```
设计：test.md + product-scope → TEST-SPEC-v3（功能+BND+EXC+PATH+SCN+NFR 齐全）
      → 自检 §七 → 定稿 → TEST-PRIORITY-v3 → 飞书/路径

执行：CI 单元/集成 → 系统层 → EXECUTION.md
验收：产品真机旅程 → 飞书填表
缺陷：五阶段 → 修复 → 回归 → CHANGELOG
```

---

# 十、链接

- [TEST-SPEC-v3.md](./TEST-SPEC-v3.md) · [TEST-SPEC-v3-SCOPE.md](./TEST-SPEC-v3-SCOPE.md)
- [ERROR-COPY-WRITING-PRINCIPLES.md](../docs/live/ERROR-COPY-WRITING-PRINCIPLES.md)
- [product-scope-final.md](../Meowmoji-CMS01/docs/product-scope-final.md)
